{
  "name": "meauxbility",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "python3 scripts/build_static.py",
    "deploy": "wrangler deploy",
    "version:upload": "wrangler versions upload"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
